allprojects {
    repositories {
        jcenter()
    }
}

group = 'io.freefair'

subprojects {
    apply plugin: 'groovy'
    apply plugin: 'maven'

    dependencies {
        compile gradleApi()
        compile localGroovy()
    }
}